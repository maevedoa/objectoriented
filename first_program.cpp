/*
08/20/2025
First program OOP
*/
#include <iostream>

// create main function
int main(void) {
    std::cout << "woof woof im a doggy" << std::endl;
    // std - standard namespace (feature that helps organize code, like a folder)
    // :: - scope resolution operator
    // cout - console output (object of an iostream class) (create object, pass data to object, prints info)
    // << - stream insertion operator (sends to the object for it to print)
    // endl - end of line (or new one)
    return 0;
}